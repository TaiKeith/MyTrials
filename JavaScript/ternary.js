console.log(true ? 1 : 2);
console.log(false ? 1 : 2);

//The value n the left "picks" which of te other 2 values will come out.
//When it's true, it chooses the middle value
//When it's false, it chooses the value on the right
